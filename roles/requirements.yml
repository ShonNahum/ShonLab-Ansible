---
# roles/requirements.yml - External Galaxy roles
# AWX auto-installs these during project sync.
# CLI: ansible-galaxy role install -r roles/requirements.yml

roles: []
# Example:
#   - name: geerlingguy.docker
#     version: "7.4.1"
#   - name: geerlingguy.pip
#     version: "3.1.0"

---
# group_vars/all.yml - Variables applied to ALL hosts in dev environment

# Connection
ansible_python_interpreter: /usr/bin/python3
ansible_user: ansible
ansible_ssh_private_key_file: ~/.ssh/id_rsa

# NTP
ntp_enabled: true
ntp_timezone: "UTC"
